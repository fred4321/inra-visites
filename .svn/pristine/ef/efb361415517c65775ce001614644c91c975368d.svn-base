<h4>MODIFIÉES : ${modifier.size()}</h4>
<ul>
    <g:each in="${modifier}" var="i" status="cpt">
        <li>MODIFICATION : Agent ${i.matricule} ${i.prenom} ${i.nom}. Ligne ${i.row}</li>
    </g:each>
</ul>